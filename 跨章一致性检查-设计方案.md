# 阶段 6：跨章事实/逻辑一致性检查 — 正式方案

> 接续《完整解决方案》。阶段 1-3 已落地（审计分级、卡片脱敏、预检修复）。本阶段补的是**审计完全没覆盖的维度**：跨章节的事实与逻辑一致性。

## 0. 背景：审计的盲区

现有审计是**权限/防剧透闸**——它查"这章能不能出现某实体""有没有提前泄密""命名合不合规"。它**不查**故事本身是否自洽。实测 5 章全部通过审计（无 P0），却带着一批硬伤：

| 错例（实测） | 类别 | 现状 |
|---|---|---|
| 第2章凶手"姓刘" vs 第3章"程行" | 属性矛盾 | 无人管 |
| 陈野第3章凭空知道"程行"全名（只学到"姓刘"） | 知识溯源 | 无人管 |
| 第4章陈野在逃 → 第5章安然配合警察问话 | 状态迁移断层 | 无人管 |
| 第3章程行报警抓陈野 → 第5章程行成"妻子失踪"报案人 | 状态迁移断层 | 无人管 |
| 第1章48h倒计时 → 第3-5章蒸发 | 线索生命周期 | 无人管 |
| 第1章锦绣花园备用钥匙 → 设而不用 | 线索生命周期 | 无人管 |
| 真林晚年龄 26 vs 30 | 属性矛盾 | 无人管 |

这些是**跨章**问题，单章审计天然看不见。

## 1. 设计定位（铁律）

1. **不新建孤立审计器、不重读全书。** 它是 **FactExtractor 写入时的"对账"步骤**：把本章抽出的结构化增量，与累积的单一叙事状态台账比对。
2. **增量，非全量。** 维护累积台账，只比对本章 delta；不是每接受一章就重审 N 章（杜绝 O(N²)）。
3. **不新增真相源。** 全部复用现有表（`facts`/`agent_knowledge`/`foreshadows`/`character_state_snapshots`/`reveal_chain`）。它是这些台账的**写入对账**，正是"从单一叙事状态投影派生"的落实。
4. **走已建的 P0/P1/P2 分级，默认 P1 不阻断。** 正文已写出，硬卡只会逼无意义重写；价值在**评审/阅读页标红**，供人决策。唯一可设硬提示的是"作者断言级属性直接矛盾"（几乎必是 bug）。

## 2. 前置：FactExtractor 升级为"带类型 + 带出处"的台账写入

这是地基，也是真正的工作量。现状抽取是自由文本 `new_facts:[{content}]`，无法比对属性。改为结构化：

```json
{
  "entity": "程行",
  "slot": "surname",            // surname/given_name/age/residence/occupation/relationship/custody/possession/...
  "value": "程",
  "chapter": 3,
  "asserted_by": "narration",   // narration=作者断言(canon) | character=角色台词(可错/可撒谎/可不可靠)
  "source_fact_id": "f_..."
}
```

`asserted_by` 是**防误报的关键**：第2章"老周说姓刘"是 `character` 断言（线人可记错/造谣），第3章婚戒内圈 CT 是 `narration`。没有这个区分，checker 会把"角色撒谎/记错"当 bug——这正是我们一路在治的"过严"。落表：复用 `facts.structured`（JSON）承载 `{slot, value, asserted_by}`，`facts.story_time` 已是章序，`facts.involved_entities` 已有实体。**几乎不加表，只丰富抽取产物。**

## 3. 四个 checker

每个对一类错例，注明判定逻辑 / 确定性 vs LLM / 复用的表。

### Checker ① 属性矛盾 —— 治 姓刘·程行 / 年龄 / 住址漂移
- **状态**：每实体每槽位 `{slot: [(chapter, value, asserted_by, fact_id)]}`，从 `facts` 派生。
- **判定**：接受第 N 章时，新断言的 `narration` 值若与已有 `narration` canon 值不同、且**无伴随"变更/揭示事件"**（`reveal_chain` 节点或显式 change-fact）→ `attribute_contradiction`（P1，姓氏/唯一性槽可升 P0-提示）。
- `character` 断言与 canon 冲突 → 仅 `unreliable_claim`（P2 提示，可能是设定中的谎言/误记，交人判）。
- **确定性**：同槽不同值是纯比对。复用 `facts`+`reveal_chain`。

### Checker ② 知识溯源 —— 治 陈野凭空知道"程行"
- **复用现成 `agent_knowledge`**（`agent_id, fact_id, learned_tick`）。缺的只是"拿它对正文断言"。
- **判定**：当 POV 在正文中**陈述或据以行动**某 fact，检查该 fact 是否已在该 POV 的 ledger 内（`learned_tick ≤ 本章`）。否则 `knowledge_provenance_violation`（P1）。
- **确定性骨架 + 轻 LLM**：候选 = FactExtractor 标记"POV 使用了 X 但 X 不在其 ledger"；LLM 仅判"正文是真在使用还是仅旁白"。

### Checker ③ 状态迁移连续 —— 治 逃犯秒变配合证人 / 报案翻转
- **复用 `character_state_snapshots`**（现 schema 在、数据空 n=0，正好启用）。FactExtractor 每章额外吐"章末状态向量"：`{custody, presence, alive, relationship, possession...}`，写 `snapshot_json`，diff 写 `changed_fields_json`。
- **判定**：第 N 章末状态必须能从第 N-1 章末状态、**经本章正文出现的迁移**到达。陈野 ch4 末=在逃、ch5 末=自由配合，本章无"脱离羁押"迁移 → `state_discontinuity`（P1）。
- **diff 锁定 + LLM 判**：状态向量 diff 锁定候选；"迁移是否在正文演出"交 LLM 判**被锁定的那一两处**，不重读全书。

### Checker ④ 线索生命周期 —— 治 48h倒计时 / 钩子没收（最机械、最先做）
- **复用 `foreshadows`**（`status, must_resolve, target_payoff_beat, payoff_discourse_pos`）——**字段已全有**，实测 21 条全 `open`、带 `target_payoff_beat=chapter:N`。
- **判定**：接受第 N 章时，扫描 `status='open' AND must_resolve AND target_payoff_beat 的章号 < N AND payoff 为空` → `dropped_foreshadow`（P1）。无截止的长线伏笔用 `max_open_span`（默认值）兜。
- **48h 倒计时**：建模成一条带硬截止 beat 的 foreshadow/thread；到点未 close 即报 `dropped_deadline`。
- **纯表扫描，零 LLM。** 数据现成，**这是最小可用原型的起点。**

## 4. LLM 粗网（残余兜底）

纯剧情逻辑（本人/表妹掩护身份滑变）任何台账都建模不了。给一个轻量 LLM pass：输入"本章正文 + ≤N 的压缩状态摘要"，只问"有无与既定事实冲突 / 逻辑断裂 / 掩护设定自相矛盾"。**不重读全书**，拿状态摘要当 ground truth。输出同进 P1/P2。

## 5. 落点 / 严重度

- 跑在**接受章节时**（FactExtractor 已在此跑），结果并入 `run_combined_chapter_audit` 的 `violations`，走 **P0/P1/P2 分级**。
- 默认 **P1 advisory 不阻断**；仅"narration 属性直接矛盾（姓氏/唯一身份）"可设硬提示。
- 维护累积台账，**只对账本章 delta**。

## 6. 分期实施

| 期 | 内容 | 依赖 | 确定性 |
|---|---|---|---|
| 6.0 | **④ 线索生命周期**（最小原型，纯表扫描） | 无（字段已全） | 纯确定性 |
| 6.1 | FactExtractor schema 升级（slot+asserted_by） | — | — |
| 6.2 | **① 属性矛盾** | 6.1 | 纯确定性 |
| 6.3 | **② 知识溯源** | 6.1 + agent_knowledge 已populated | 骨架确定 + 轻LLM |
| 6.4 | **③ 状态迁移**（启用 state_snapshots） | 6.1 | diff确定 + LLM判迁移 |
| 6.5 | LLM 粗网 | — | LLM |

## 7. 验收 / 测试

- 6.0：构造一个"target_payoff_beat=chapter:2、到 ch3 仍 open"的伏笔 → checker 报 `dropped_foreshadow`；已 payoff 的不报。对现有项目跑出 21 条逾期清单。
- 6.2：注入"ch2 narration=姓刘、ch3 narration=程"→ 报 `attribute_contradiction`；"ch2 character=姓刘"则只报 `unreliable_claim`。
- 6.3：POV 使用不在其 ledger 的 fact → 报 `knowledge_provenance_violation`。
- 全部走现有 severity 测试框架。

## 8. 诚实的边界

- **只和台账一样准**：FactExtractor 抽错/漏抽就查不出。所以 6.1（带类型+出处的抽取）是地基，checker 反而是薄表比对。
- 纯"情节没说服力/查案太顺/过度重复"超出一致性范畴，归风格/结构（已有 `_gram_overlap` 接一部分）。
- 不替代人工审稿；目标是把**机械可查的硬伤**自动标红，把人的注意力留给真正的创作判断。
