# P4 实施提示词 · 统一渐进披露模型

> 用法：新开对话，把本文件整段贴进去（或直接说"读 P4-渐进披露模型-实施提示词.md 后开工"）。

---

## 0. 背景（新对话先读）

先读 `README.md`、`docs/continuation-workshop.md`、`续写.txt`，再读 memory 里的 `chapter-leak-fix.md`。

这是一个**长篇小说续写工程系统**，核心是把影响连续性的状态拆成结构化数据（世界配置/知识图谱/人物卡/文风画像/章节计划），让模型在被约束的上下文里续写。必须保持**通用性**：任何具体作者/作品（如《龙族》《我给死人做售后》）都只是测试样本，不能反向定义系统。

诊断样本：`server/.data/projects/proj_53ab5edd.db`（《我给死人做售后》，林晚谜底 = 丈夫 + 整容替身合谋杀害、尸骨埋别墅地下室、替身冒名继承遗产）。LLM 配置在 `server/.data/config.json`（DeepSeek）。前后端启动：`python -m uvicorn server.app:app --port 8000` + `cd frontend && npm run dev -- --port 5180`。

## 1. 已完成（P1–P3 + P2.5），勿重复

"章节泄露"（每章把后文谜底/未来人物道具写进 prompt）的四向量里，**prompt 注入侧已全部修复并端到端验证**：

- **P1**：`build_context`（`src/novel_engine/narration/retrieval.py`）解耦 `prose_safe`/`allowed_entity_ids`/`include_faction_overview` 三关注点。写作 `_rag_context`（scene_writer.py）传 `chapter_seq+exclude_future+本章白名单`；规划（planner.py ~1869）传 `chapter_seq+exclude_future`（不上硬白名单以保图谱扩展）。→ 人物小传/谜底、未来图谱道具不再进 prompt。
- **P2**：`compile_chapter_package`/`_future_rows`（chapter_scope_validator.py）当前章去重升级为模糊重叠 `_marker_overlaps_text`；`may_mention` 只从本章 allowed 集派生（不再扫 beat 文本任意实体名）。
- **P2.5**：`build_prose_chapter_scope` 写手契约 `forbidden` 置空（不再下发未来剧情明细，避免"报菜名"泄露未来人物名）；auditor 独立重算契约不受影响。
- **P3**：`DraftManager._redact_plan_for_rewrite`（story_bible/drafts.py）重写前据 forbidden marker（≥6 字公共子串）+ 未授权实体名（≥3 字）摘掉越界 beat/道具，redacted plan 同时驱动重写与重审。

**当前残留（P4 要根治的）**：
1. ch2 仍 blocked 于 `new_investigation_result: 死因` —— 因**遗留脏 plan** 的 exit_state 把 ch2 推向"调查死亡"；P1 之后重新规划的章不会这样，但已存的脏 plan 需要重排或披露日程约束。
2. 林晚 `one_liner`（"投诉丈夫和整容替身冒用她的脸"）始终注入、不受 prose_safe 管，premise 级微泄。
3. 更根本：**地点/势力没有"某章后才出现"的机制**；现有 `available_from_chapter` 是二值开关（0→全量 detail），实体登场必然突兀。

## 2. P4 目标：把二值披露闸门升级成 4 态时间线（通用于人物/地理/势力/道具/真相）

现状散落的披露机制（`available_from_chapter`、faction `source=='w3_far'`、`Fact.story_time`、`inventory.acquired_chapter`）都是 0/1 开关，缺中间"伏笔态"，所以要么全藏要么突然砸出。P4 给每个可披露对象一张**披露日程**：

| 阶段 | 条件 | prompt 可注入 | 解决 |
|---|---|---|---|
| 0 隐藏 | `ch < foreshadow_from` | 无 | 防泄底 |
| 1 伏笔 | `foreshadow_from ≤ ch < reveal_chapter` | **只 `foreshadow_hint`**（征兆/传闻/间接效果，不点名不解释不给谜底） | **防突兀** |
| 2 登场 | `reveal_chapter ≤ ch` | 公开 detail（= 现在 prose_safe=True 那层） | 正式出场 |
| 3 揭秘 | `ch ≥ secret_reveal_chapter` | 公开 detail + `secret_truth` | 谜底揭开 |

> 注：上次说的"人物 backstory 公开/机密分层"= 阶段2 vs 阶段3 的特例，这里一般化到所有实体类型，并补上缺失的阶段1。

## 3. 落点（实施清单，按依赖排序；file:line 需对当前代码核对，可能漂移）

### 4a · 数据模型 + 渲染（additive，低风险，向后兼容）

- `src/novel_engine/models.py`：`CharacterCard`/`Location`/`Faction` 各加
  `foreshadow_from:int=0`、`reveal_chapter:int=0`、`secret_reveal_chapter:int=0`、`foreshadow_hint:str=""`、`secret_truth:str=""`。空值=现行为（不破坏既有项目）。
- `src/novel_engine/db.py`：`ALTER TABLE` 幂等加列（沿用现有 migration 风格）。无需回填既有数据——空值落到阶段2，等同现状。
- `src/novel_engine/narration/retrieval.py`：把 `_available_in_chapter`（二值）升级成 `_disclosure_stage(repo, eid, ch) -> 0|1|2|3`；`_card_snippet`/`_location_snippet`/`_faction_snippet`/`_object_snippet` 依阶段选字段：
  - 阶段0→空串；阶段1→只吐 `foreshadow_hint`；阶段2→公开 detail（含 one_liner，但 one_liner 对 mystery 实体也应只放公开面）；阶段3→公开+`secret_truth`。
  - 一个函数统一管人物/地理/势力，替换散落的 `prose_safe` 二分。

### 4b · 契约 3 桶 + 新违例

- `chapter_scope_validator.py`：`compile_chapter_package` 输出 `allowed_full`(阶段2实体) / `allowed_hint`(阶段1实体) / 其余 forbidden。
- `validate_chapter_scope` 新增两类违例：
  - 阶段1实体被**点名/详写**（超出 hint）→ `premature_reveal`；
  - 阶段2实体**之前没埋过伏笔**（见 4c 不变式）→ `unforeshadowed_introduction`。

### 4c · planner 自动排期 + 埋伏笔（A 打底）

- 锁定/规划 parts 时（`planner.py`），为每个"非开场"实体（开场 cast/地点之外的）分配 `foreshadow_from/reveal_chapter`，并**在登场前自动插一条 `Foreshadow`**（复用 `models.py` 现有 `Foreshadow` 台账：`planted_discourse_pos`/`payoff_discourse_pos`/`status`）。
- **公平性铁律**：`foreshadow_hint` 只能从实体**公开面**生成，严禁引用 `secret_truth`（否则伏笔本身剧透）。
- **硬不变式**：实体要进入阶段2，要求 `[foreshadow_from, reveal_chapter)` 内至少有一条指向它、status≠abandoned、planted < 登场章的伏笔，否则 4b 报 `unforeshadowed_introduction`。这把"天命咨询公司"等长线反派强制为"先埋征兆、后登场"。

### 4d · 前端披露时间线（B 兜底）

- 蒸馏/锁定时生成披露日程草案，前端给一个"披露时间线"面板让用户覆盖个别实体的 `reveal_chapter` 再锁定。落点参考 `frontend/src/views/WorldConfig.tsx` 及现有人物/地点/势力面板。

## 4. 同时修两个遗留

- 残留(1)：对**已存的脏 plan**，要么提供"重新规划单章"入口（P1 后 planner 不再看谜底），要么让披露日程约束 exit_state 不引用未到披露章的实体。
- 残留(2)：mystery 角色的 `one_liner` 也要走公开/机密拆分（阶段3前只给公开版）。

## 5. 顺带（可选，已有 spawn_task 记录）

`Repository.item_exists` 未定义但 `planner.py:1145/1366` 直调，导致约 5 个库存测试失败。按 inventory status（held|transferred|lost|consumed|destroyed|sacrificed）判物品是否仍存在来补全。测试有大批 `Narrator` 导入残留收集错误（预存），跑测试加 `--continue-on-collection-errors`。

## 6. 验证

1. 单测：`pytest tests/ -k "scope or chapter_audit or retriev or continuation or draft" --continue-on-collection-errors`。
2. 离线：对 proj_53ab5edd 调 `compile_chapter_package`/`build_context`，确认阶段1实体只出 hint、阶段0实体不出现、阶段3前 secret_truth 不进 prompt。
3. 端到端：重启后端，`POST /api/projects/proj_53ab5edd/chapters/drafts` 重生成 ch2，确认 draft 不再 blocked、`llm_logs` 里写手 prompt 无谜底/未来实体、且新登场实体在 prompt 里能看到**先行伏笔**而非凭空出现。

## 7. 原则

先给方案再动代码；优先高收益、能闭环验证的改动；改完跑测试+最小验证并汇报剩余。保持通用，别把系统写死在某作者/某作品上。
